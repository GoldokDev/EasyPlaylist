# Journal de validation

Ajouter une entrée après chaque validation significative. Ne pas déclarer de preuve visuelle sans capture réellement produite et examinée. Ne pas déclarer une intégration fournisseur réelle à partir d'un fake.

## Modèle

```text
## V-XXX — <item ou jalon>

- Date : YYYY-MM-DD
- Version/commit : <hash ou working tree>
- Environnement : <OS, runtimes, Docker, navigateurs utiles>
- Périmètre : <règles, services et écrans touchés>
- Données : <fixtures, comptes de test anonymisés ou N/A>
- Commandes : <commandes exactes et codes de sortie>
- Parcours : <actions API, temps réel et navigateur>
- Logs : <erreurs, avertissements et contrôle des secrets>
- Captures : <chemins examinés ou aucune>
- Critères : <mapping critère -> preuve>
- Résultat : PASS / FAIL / PARTIAL
- Notes : <risques, limites ou prochaine action>
```

## V-000 — Gouvernance initiale

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows, dépôt vide avant cadrage
- Périmètre : README, AGENTS, documentation canonique, backlog et skills locaux
- Données : audit en lecture seule d'EasyParty et de l'organisation td-roguelike
- Commandes : `git diff --check` (code `0`) ; validation PowerShell des liens Markdown, frontmatters, métadonnées `openai.yaml`, longueurs et invariants du backlog (code `0`)
- Parcours : 14 fichiers Markdown parcourus ; liens locaux résolus ; skills `deliver-backlog-item` (57 lignes) et `validate-web-delivery` (76 lignes) validés ; exactement un item `READY` et aucun `IN_PROGRESS`
- Logs : aucun secret applicatif ; aucun code applicatif présent
- Captures : aucune, changement documentaire sans interface
- Critères : structure demandée présente ; état du projet et décisions ouvertes explicites
- Résultat : `PASS`
- Notes : le générateur et le validateur Python officiels du skill-creator n'ont pas pu être exécutés, car l'environnement expose uniquement le lanceur Microsoft Store sans runtime Python. Les mêmes contraintes structurelles ont été contrôlées localement. Aucun build applicatif ou Docker n'est revendiqué : le dépôt reste volontairement au stade de cadrage.

## V-001 — DISC-001, contrat produit de lecture

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows, validation documentaire PowerShell ; aucun `package.json` ou `compose.yaml` par conception
- Périmètre : lecture navigateur, partage des connexions, file unique, Spotify/Deezer, site public, collaboration ouverte, expiration
- Données : réponses utilisateur aux six questions de cadrage ; hypothèses réversibles pour accès des invités aux connexions et expiration à 24 heures
- Commandes : validation PowerShell des espaces, liens Markdown, huit statuts produit, décisions acceptées, preuves textuelles et invariant d'un unique item `IN_PROGRESS` (code `0`) ; `git diff --check` (code `0`)
- Parcours : 17 fichiers contrôlés, 14 fichiers Markdown parcourus, huit questions sur huit résolues ; absence attendue de harnais applicatif confirmée
- Logs : aucune erreur ; warning Git local hors dépôt sur `C:\Users\sajid\.config\git\ignore`, sans impact sur les fichiers
- Captures : aucune, changement documentaire sans interface
- Critères : Q-001 à Q-008 documentent toutes les réponses ; D-006, D-008 à D-015 sont `ACCEPTED` ; vision, MVP, architecture et backlog reflètent le contrat
- Résultat : `PASS`
- Notes : aucune capacité Spotify ou Deezer n'est encore revendiquée. Cette preuve relève de `DISC-002` et devra utiliser des sources officielles actuelles.

## V-002 — DISC-002, faisabilité officielle des fournisseurs

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; navigation web sur documentations officielles ; aucun runtime, package applicatif ou Compose présent avant `FOUND-001`
- Périmètre : matrice Spotify, Deezer, SoundCloud et YouTube/YouTube Music ; OAuth, recherche, lecture web/distante, abonnements, quotas, stockage, production, callbacks, comptes de test et conformité du partage au lobby
- Données : 30 URL officielles distinctes vérifiées le 2026-08-08 ; aucune credential, aucun compte fournisseur et aucun test réel
- Commandes : `git diff --check` (code `0`) ; contrôle PowerShell de 15 fichiers Markdown, liens locaux, espaces finaux, champs obligatoires de la matrice, état `VERIFY`, blocage de `FOUND-001` et décision `D-016` (code `0`) ; scan `rg --pcre2` de motifs de clés privées, clés Google et tokens OAuth (code `0`) ; contrôle de l'absence attendue de `package.json`, `compose.yaml`, `apps` et `packages` (code `0`)
- Parcours : lecture croisée des références Spotify Web API/Web Playback/quotas/policies, conditions et canal officiel Deezer, API/quotas/conditions SoundCloud, YouTube Data/IFrame/policies/quotas/Data Portability ; 30 liens externes officiels uniques recensés dans `Docs/architecture/provider-feasibility.md`
- Logs : aucune erreur de validation ; aucun motif de secret ; le warning Git global hors dépôt observé précédemment n'affecte pas ce contrôle
- Captures : aucune, livraison documentaire sans interface ; aucune preuve navigateur fournisseur n'est revendiquée
- Critères : matrice datée couvrant quatre familles (`provider-feasibility.md`) ; chaque fournisseur possède des sources officielles et un verdict ; OAuth, recherche, lecture, abonnement, quotas, stockage et production sont couverts dans la table et les analyses ; Spotify et Deezer sont détaillés en priorité avec comptes et matériel ; les absences d'API ou permissions sont marquées `NON PROUVÉ`/`BLOQUÉ` ; l'incompatibilité avec le partage/public est remontée sans changer D-012 ; Q-009 et D-016 isolent l'arbitrage utilisateur restant
- Résultat : `PARTIAL`
- Notes : la preuve documentaire est complète, mais l'item reste `VERIFY` jusqu'au choix utilisateur. Spotify est limité à un prototype privé et son modèle de délégation n'est pas prouvé ; Deezer est incompatible avec un lobby public entre amis et n'ouvre plus de nouveaux accès particuliers ; SoundCloud interdit l'agrégation de lecture ; YouTube exige une vidéo visible et un audit au-delà de 100 recherches quotidiennes. Les tests applicatifs, Docker, API, temps réel et navigateur sont hors périmètre et impossibles avant le scaffold.

## V-003 — DISC-002, arbitrage Spotify organisateur

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; validation documentaire locale
- Périmètre : remplacement du partage multi-comptes et de Spotify/Deezer par une bêta Spotify portée par le créateur ; report de Deezer ; dépendances du backlog
- Données : arbitrage explicite de l'utilisateur dans la conversation ; matrice officielle V-002
- Commandes : `git diff --check` (code `0`) ; contrôle PowerShell de 15 fichiers Markdown, liens locaux, espaces finaux, D-017 `ACCEPTED`, DISC-002 `DONE`, FOUND-001 `READY`, absence de PROVIDER-003 dans le MVP et clauses produit obligatoires (code `0`) ; scan `rg --pcre2` de clés privées et tokens (aucun résultat, code interprété `0` par le script)
- Parcours : vision, MVP, questions, architecture, modèle fournisseur, matrice, décisions et backlog relus après synchronisation
- Logs : aucune erreur ; aucun motif de secret
- Captures : aucune, changement documentaire sans interface
- Critères : la matrice et les sources restent inchangées ; le mode exact Spotify est accepté ; le créateur est seul propriétaire OAuth ; les invités sans compte Spotify recherchent et commandent via EasyPlaylist ; Deezer est reporté à POST-007 ; FOUND-001 est débloqué
- Résultat : `PASS`
- Notes : D-017 remplace D-011 et D-012, résout D-016/Q-009 et ferme DISC-002. Le mode Development autorise une bêta fermée, pas une connexion Spotify libre-service.

## V-004 — FOUND-001, monorepo et Docker Compose

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; images Node.js 24 Alpine, nginx 1.28 Alpine et PostgreSQL 18.4 Alpine
- Périmètre : workspaces `apps/web`, `apps/api` et `packages/contracts`, contrats de santé Zod, écran de démarrage responsive, Dockerfiles, Compose, documentation de lancement et décision D-018
- Données : volume vierge `easyplaylist-found001-validation_postgres_data`, créé automatiquement par l'image officielle PostgreSQL ; aucune migration n'existe encore dans le périmètre de FOUND-001
- Commandes : installations npm ciblées puis `npm.cmd ci` (code `0`) ; `npm.cmd run typecheck` (code `0`) ; `npm.cmd run build` (code `0`) ; `npm.cmd ls --depth=0 --workspaces --include-workspace-root` (code `0`) ; `docker compose config` (code `0`) ; `docker compose build` (code `0`) ; `docker compose -p easyplaylist-found001-validation up -d --build --wait` (code `0`) ; `docker compose -p easyplaylist-found001-validation ps` (trois services healthy) ; contrôles `curl.exe` de liveness/readiness ; `git diff --check` (code `0`) ; contrôle de 15 fichiers Markdown et liens locaux (code `0`) ; scans `rg` des espaces finaux et motifs de secrets (aucun résultat)
- Parcours : démarrage sur base vierge ; `GET /health/live` retourne `200` et `{service:api,status:ok}` ; `GET /health/ready` retourne `200` et `database:up` ; arrêt de `db`, readiness `503` et `database:down` tandis que liveness reste `200` ; l'identifiant du conteneur API reste `9876ce12…` avant/après la panne ; redémarrage de `db`, retour des trois healthchecks et readiness `200` ; chargement réel de `/`, des assets et de `/api/health/ready` dans Chrome
- Logs : aucune erreur console navigateur ; requêtes document, CSS, JavaScript et readiness toutes en `200` ; l'arrêt volontaire de PostgreSQL produit uniquement le warning API `{databaseErrorCode:57P01}` et des readiness `503`, sans crash ni détail de connexion ; aucun motif de secret détecté dans le dépôt
- Captures : `artifacts/validation/found-001-mobile.png`, Chrome headless en viewport responsive 500 × 1500, capture examinée : contenu complet, ordre visuel clair, aucun débordement et état « Socle prêt » visible
- Critères : arborescence requise prouvée par les trois workspaces ; choix et versions consignés dans D-018 et l'architecture ; Compose prouvé sur volume vierge avec réseaux, volume et trois healthchecks ; variables documentées dans `.env.example` ; écran et deux sondes exercés réellement ; dépendance de readiness à PostgreSQL prouvée par le scénario `200 → 503 → 200` ; commande `docker compose up --build` documentée dans le README
- Résultat : PASS
- Notes : le premier arrêt contrôlé de PostgreSQL a révélé un événement `pg` non géré puis un log trop détaillé ; les deux défauts ont été corrigés et le scénario final a été rejoué sur l'image reconstruite. FOUND-002 est promu `READY` pour ajouter formatage, lint, tests automatisés et Playwright.

## V-005 — FOUND-002, harnais qualité et validation

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; images Node.js 24 Alpine et nginx 1.28 Alpine
- Périmètre : scripts racine de qualité, tests Fastify, journalisation, sonde rouge, fixture PostgreSQL, orchestration Docker, parcours Playwright et capture mobile
- Données : schéma PostgreSQL `test_<uuid>` dans un projet Compose jetable ; bases `easyplaylist_test`, `easyplaylist_e2e` et `easyplaylist_failure` avec mots de passe locaux factices ; tous les volumes ont été supprimés par les orchestrateurs ou le `finally` de validation
- Commandes : `npm.cmd install --save-dev prettier oxlint vitest @playwright/test` (code `0`, 0 vulnérabilité) ; `npm.cmd run format:check` (code `0`) ; `npm.cmd run lint` (code `0`, aucun warning) ; `npm.cmd run typecheck` (code `0`) ; `npm.cmd run test` (code `0`, 3 tests) ; sonde `vitest ... vitest.failure.config.ts` (code attendu `1`) via `npm.cmd run test:failure-probe:verify` (code `0`) ; `npm.cmd run test:postgres` (code `0`, 1 test, volume supprimé) ; `npm.cmd run build` (code `0`) ; `docker compose config` et override `docker compose -f compose.yaml -f compose.test.yaml config` (codes `0`) ; `docker compose build` (code `0`) ; `npm.cmd run e2e` (code `0`, 1 test, volume supprimé) ; scan `rg --pcre2` de clés privées, clés API et tokens (aucun résultat)
- Parcours : l'E2E lance les trois services avec healthchecks sur des ports de test, charge `/` dans Chrome en 390 × 844, attend « Socle prêt », vérifie `/api/health/ready` à `200`, l'absence de débordement horizontal et d'erreur console, puis capture la page ; un parcours Docker séparé prouve readiness `200 → 503` après arrêt réel de PostgreSQL et liveness maintenue à `200`
- Logs : les requêtes Fastify portent `requestId` ; un test unitaire injecte authorization, cookie, access token, refresh token et token générique puis prouve leur remplacement par `[Redacted]` et l'absence des valeurs ; les logs Docker finaux ne contiennent ni exception applicative ni credential ; l'arrêt volontaire de PostgreSQL produit seulement le code borné `57P01` et le message de readiness ; les warnings `locale`/auth locale viennent de l'initialisation officielle PostgreSQL Alpine dans des volumes jetables
- Captures : `artifacts/validation/found-002-mobile.png`, 390 × 1013, SHA-256 `BEB514C335AC1198DFDF34DA335619A9CB5954C40A323DC668962DE95E799717`, examinée : titre, promesse, état « Socle prêt » et trois cartes lisibles, sans clipping ni débordement
- Critères : scripts racine prouvés par format/lint/typecheck/test/build/E2E ; sonde rouge isolée prouvée par code `1` et wrapper vert ; fixture prouvée par création, lecture, suppression du schéma puis suppression du volume ; Playwright prouvé sur stack Docker et capture examinée ; identifiant de requête visible dans les logs ; redaction prouvée sur cinq formes sensibles ; commandes réelles synchronisées dans `.agents/skills/validate-web-delivery/SKILL.md`
- Résultat : `PASS`
- Notes : `typescript-eslint` 8.66.0 a refusé TypeScript 7 (`peer < 6.1`) pendant l'installation initiale, sans modifier le lockfile final ; D-019 retient Oxlint. Le réseau backend produit reste interne ; `compose.test.yaml` le rend non interne uniquement pour permettre au test PostgreSQL hôte de joindre sa base jetable. Aucun schéma métier ni migration n'existe encore : ils relèvent de `FOUND-003`, désormais `READY`.

## V-006 — FOUND-003, persistance, identité et coffre de secrets

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; images Node.js 24 Alpine et nginx 1.28 Alpine
- Périmètre : deux migrations SQL, contraintes lobby-scopées, runner transactionnel et checksums, endpoint d'identité invitée, cookie HMAC, coffre AES-256-GCM versionné, contrats publics, configuration Docker et documentation
- Données : schémas PostgreSQL `test_<uuid>` et projets Compose `easyplaylist-pg-tests-<pid>`, `easyplaylist-e2e-<pid>` et `easyplaylist-found003-validation`, tous jetables ; UUID et credentials factices dans les tests ; volumes supprimés en `finally`
- Commandes : `npm.cmd run format:check`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run test` (11 tests), `npm.cmd run test:failure-probe:verify` (code rouge interne attendu `1`, wrapper `0`), `npm.cmd run test:postgres` (3 tests), `npm.cmd run build`, `docker compose config`, `docker compose build`, `npm.cmd run e2e` (1 parcours), smoke Compose/PowerShell sur ports dédiés, scans `rg` du dépôt et du bundle web (tous codes finaux `0`)
- Parcours : le runner applique `001_initial_foundation.sql` puis `002_lobby_activity.sql` sur schéma vide, un second passage n'applique rien, et le chemin représentatif applique `001`, conserve un lobby puis ajoute/backfill `last_activity_at` par `002` ; la stack Docker migre avant l'écoute et ses trois services deviennent healthy ; `POST /identity/guest` retourne `200` et un cookie `HttpOnly`/`SameSite=Lax`, le second appel reprend le même participant, une valeur altérée est remplacée ; l'arrêt réel de PostgreSQL produit readiness `503` avec liveness `200`, puis readiness `200` après reprise
- Logs : chaque requête API porte `requestId` ; l'arrêt volontaire PostgreSQL ne journalise que le code borné `57P01` et l'échec de readiness ; cookies, credentials, ciphertexts, access/refresh tokens et secrets sont masqués par test unitaire ; aucun motif de clé privée/token n'est trouvé dans le dépôt et aucun identifiant sensible n'est trouvé dans `apps/web/dist` ; aucune exception applicative inattendue dans les logs finaux
- Captures : `artifacts/validation/found-002-mobile.png`, 390 × 1013, SHA-256 `BEB514C335AC1198DFDF34DA335619A9CB5954C40A323DC668962DE95E799717`, régénérée et examinée après l'E2E : contenu complet, contraste et hiérarchie inchangés, sans clipping ni débordement ; aucune interface n'a changé dans FOUND-003
- Critères : les neuf tables attendues, dont lobbies, participants, memberships, connexions, file, bail, état de lecture et reçus, sont listées après migration ; identité opaque, signature, attributs de cookie, reprise, altération et expiration sont couvertes par unités, E2E et smoke réel ; deux memberships portent le même pseudonyme tandis que les références composites utilisent les UUID ; le coffre AES-256-GCM expose une version et refuse ciphertext, tag ou version altérés ; le schéma Zod public supprime structurellement access token, refresh token et enveloppe chiffrée ; base vide et upgrade `001 → 002` sont prouvés ; les FK refusent auteur et propriétaire provenant d'un autre lobby (`23503`), l'expiration lobby vaut 24 heures et les scans/logs restent sans secret
- Résultat : `PASS`
- Notes : un premier smoke PowerShell a reçu le `415` Fastify attendu car `Invoke-WebRequest` ajoutait un type formulaire à un POST vide ; le parcours final a été rejoué avec JSON explicite et a passé toutes les étapes. D-020 consigne l'absence volontaire d'ORM, les contraintes composites, HMAC-SHA-256 et AES-256-GCM. `LOBBY-001` et `PROVIDER-001` deviennent `READY` ; l'ordre du backlog désigne `LOBBY-001` comme prochain item.

## V-007 — LOBBY-001, créer et rejoindre un lobby

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; images Node.js 24 Alpine et nginx 1.28 Alpine
- Périmètre : contrats Zod de lobby, génération de codes, opérations PostgreSQL atomiques, routes `POST /lobbies`, `POST /lobbies/join` et `GET /lobbies/:id`, reprise par cookie, interface React mobile/desktop, lien profond et erreurs publiques
- Données : schémas PostgreSQL et projets Compose jetables `easyplaylist-pg-tests-9716` et `easyplaylist-e2e-26700` ; lobbies, UUID, noms et codes générés uniquement pour les tests ; volumes supprimés en `finally`
- Commandes : `npm.cmd run format:check`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run test` (5 fichiers, 24 tests), `npm.cmd run test:failure-probe:verify` (code rouge interne attendu `1`, wrapper `0`), `npm.cmd run test:postgres` (3 fichiers, 4 tests), `npm.cmd run build`, `docker compose config`, `npm.cmd run e2e` (1 parcours, 2 contextes participants plus 1 contexte d'échec), scan `rg --pcre2` de clés privées et tokens ; tous les codes finaux sont `0`
- Parcours : l'organisateur crée « Anniversaire de Léa », reçoit un code de six caractères non ambigus et devient créateur ; un second contexte ouvre le lien, rejoint comme Noor et voit deux membres ; les deux identités retrouvent leur membership après rafraîchissement ; un troisième contexte reçoit l'erreur française bornée pour un code inconnu ; l'intégration PostgreSQL prouve en plus isolation inter-lobby et réponses identiques pour code inconnu, fermé ou expiré
- Logs : toutes les requêtes API portent `requestId` ; succès `201/200`, refus inter-lobby et code indisponible `404` attendus ; aucune exception ou erreur console dans la passe finale ; aucun secret détecté ; seuls les avertissements `locale` et authentification locale de l'image PostgreSQL jetable sont présents
- Captures : `artifacts/validation/lobby-001-entry-mobile.png`, 390 × 1103, SHA-256 `AA236E57D10D3928E8C4DE5541D93DB7577E02157E54824BDF9C89B068314ED1` ; `artifacts/validation/lobby-001-joined-mobile.png`, 390 × 844, SHA-256 `99FCDD0398AB1BB9B881B058A02B852CEEAF0C21A4E24FE9625BE0E3C135278C` ; `artifacts/validation/lobby-001-lobby-desktop.png`, 1280 × 900, SHA-256 `3A03A27BC4663970069292FE730AD0A418FF476D33A32832A4F3BB4D5ADE17F4` ; toutes ouvertes et examinées, hiérarchie, contrastes, champs, code, URL et actions lisibles, sans clipping ni débordement
- Critères : création nommée et rôle créateur prouvés par API, base et E2E ; alphabet et collision couverts par contrats/unités, code et lien visibles dans les captures ; jonction validée par API réelle et second navigateur ; erreurs inconnue/fermée/expirée identiques en intégration et état d'erreur visible en E2E ; reprise prouvée sur les deux navigateurs ; isolation refusée en `404` par PostgreSQL/API ; parcours mobile, focus clavier, console, réseau et captures inspectés
- Résultat : `PASS`
- Notes : la validation a révélé puis fait corriger la résolution obsolète du package de contrats dans Vitest intégration, l'inférence ambiguë des paramètres temporels PostgreSQL (`42P08`) et le comptage d'un membership créé dans un CTE. D-021 consigne le contrat public final. `PROVIDER-001` reste le prochain item `READY` ; `SEARCH-001` attend désormais uniquement sa livraison.

## V-008 — PROVIDER-001, contrat et fournisseur fake

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; images Node.js 24 Alpine et nginx 1.28 Alpine
- Périmètre : port fournisseur neutre, garde de capacités, fake déterministe, résumé public de connexion, route lobby-scopée `GET /lobbies/:id/providers`, panneau responsive et documentation D-022
- Données : adaptateur fake sans réseau, compte, secret ni persistance ; horloge injectée pour la fin de titre ; scénarios `success`, `partial_failure`, `expired` et `unavailable` ; projets Compose et volumes PostgreSQL jetables supprimés par les harnais
- Commandes : `npm.cmd run format:check`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run test` (8 fichiers, 38 tests), `npm.cmd run test:failure-probe:verify` (code rouge interne attendu `1`, wrapper `0`), `npm.cmd run test:postgres` (3 fichiers, 4 tests), `npm.cmd run build`, `docker compose config`, `docker compose build`, `npm.cmd run e2e` (1 parcours), scans `rg` du dépôt et du bundle web ; tous les codes finaux sont `0`
- Parcours fournisseur : la suite réutilisable vérifie capacités uniques, recherche stable, résultat normalisé, résolution, démarrage, fin de titre, rafraîchissement et révocation ; les tests fake ajoutent résultats avec panne partielle, refus sur credentials expirés puis reprise après refresh, indisponibilité complète et titre disparu ; la garde centrale prouve par spies qu'une capacité absente n'appelle ni lecture ni refresh
- Parcours API : un membre reçoit `200` et uniquement capacités, statut et limites publiques ; une autre identité reçoit `404 LOBBY_NOT_FOUND` sur la stack réelle ; le DTO et les scans excluent access token, refresh token, enveloppe chiffrée, propriétaire interne, ciphertext et auth tag
- Parcours navigateur : l'organisateur crée puis reprend le lobby, voit le mode simulation, sept capacités et les trois limites ; une interception `503` affiche l'état dégradé attendu, dont l'unique erreur console réseau est vérifiée puis isolée ; le parcours complet création/jonction/reprise reste vert sans autre erreur console ni débordement horizontal
- Logs : requêtes réelles provider en `200` et refus inter-lobby en `404`, chacune avec `requestId` ; aucune exception applicative inattendue ; seuls les avertissements `locale` et authentification locale de l'image PostgreSQL jetable sont présents ; aucun motif de secret dans le dépôt ou champ de credential dans `apps/web/dist`
- Captures : `artifacts/validation/provider-001-fake-mobile.png`, 390 × 1076, SHA-256 `54D343F6F1846E63B5713516F2127424D9E5F835B38FFA168CEADC7160A10F39` ; `artifacts/validation/lobby-001-lobby-desktop.png`, 1280 × 900, SHA-256 `342C1CFA1A213326779947D1C7EBFF847C34280C1326A135AE5547E7CA522473` ; toutes deux ouvertes et examinées : simulation, capacités et limites lisibles, contraste cohérent, aucun clipping ni débordement
- Critères : port complet prouvé par types et suite commune ; capacités effectives et non-appel des absentes prouvés en unité ; six comportements fake requis prouvés par contrat et scénarios ; aucun type SDK ou spécifique Spotify dans lobby, queue ou playback ; la suite commune est enregistrée pour l'unique adaptateur actuel ; limites visibles et documentées sans revendiquer Spotify
- Résultat : `PASS`
- Notes : un premier appel Vitest comportait une option Jest invalide, sans défaut applicatif. Le premier E2E enrichi a correctement produit le `503` simulé mais le considérait encore comme une erreur console inattendue ; le test a été corrigé pour exiger exactement cette erreur pendant l'état dégradé, la vider, puis le rejeu final a passé. L'accès Docker hors sandbox a été autorisé pour les harnais. Aucune migration, dépendance ou preuve fournisseur réelle n'a été ajoutée. `SEARCH-001` devient le prochain item `READY`.

## V-009 — SEARCH-001, recherche agrégée

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; images Node.js 24 Alpine et nginx 1.28 Alpine
- Périmètre : contrats Zod de recherche, `CatalogSearchService`, route autorisée `GET /lobbies/:id/search`, branchement du fake, déduplication et variantes, erreurs partielles bornées, interface responsive, documentation et décision D-023 ; aucune migration ni dépendance ajoutée
- Données : connexion virtuelle fake sans secret ; schémas PostgreSQL et projets Compose jetables `easyplaylist-pg-tests-9068`, `easyplaylist-e2e-20088` et prédécesseurs de validation, tous supprimés avec leurs volumes par les harnais
- Commandes finales : `npm.cmd run format:check`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run test` (9 fichiers, 48 tests), `npm.cmd run test:failure-probe:verify` (code rouge interne attendu `1`, wrapper `0`), `npm.cmd run test:postgres` (3 fichiers, 4 tests), `npm.cmd run build`, `docker compose config`, `docker compose build`, `npm.cmd run e2e` (1 parcours), scans `rg` du dépôt et de `apps/web/dist` ; tous les codes finaux attendus sont obtenus
- Domaine : les tests injectent deux sources différées et prouvent que leurs recherches commencent avant résolution ; zéro connexion consentie/capable n'appelle aucun adaptateur ; un même ISRC produit un résultat avec deux variantes, tandis qu'un même titre porté par un autre artiste reste séparé ; les issues partielles et pannes conservent les succès ; une promesse bloquée devient `PROVIDER_SEARCH_TIMEOUT` après le timeout injecté ; curseur et limite atteignent chaque adaptateur
- Bornes et confidentialité : requête de 2 à 100 caractères, page de 1 à 20 et curseur de 200 caractères validés par contrat ; provider, connexion, identifiant source, métadonnées et disponibilité sont présents par variante ; erreurs et messages d'adaptateur bruts sont remplacés par des codes/messages bornés ; les DTO retirent les champs de credential ; aucun motif de clé privée/JWT n'est trouvé dans le dépôt et les trois fichiers du bundle web sont sans `accessToken`, `refreshToken`, `encryptedCredentials`, `ciphertext` ou `authTag`
- API/PostgreSQL : un membre obtient `200` et deux résultats fake avec `limit=2`, une requête courte et une page à `21` obtiennent `400`, et une identité d'un autre lobby obtient `404` avant tout appel fournisseur ; les migrations existantes continuent de passer sur base isolée et les logs portent `requestId` sans exception applicative inattendue
- Navigateur : la stack Docker vierge devient healthy, l'organisateur recherche `midnight` et voit trois titres avec source fake et lecture disponible ; une réponse `200` avec issue partielle conserve `Partial but useful` et affiche un avertissement ; création, reprise, jonction, erreur fournisseur précédente et isolation restent vertes ; aucune erreur console inattendue ni débordement horizontal
- Captures : `artifacts/validation/search-001-results-mobile.png`, 390 × 1719, SHA-256 `C23491A53E919BEE68A9B4DEC5882D06BDABD3F59FE9C7490592D63358F8125A` ; `artifacts/validation/search-001-partial-mobile.png`, 390 × 1582, SHA-256 `D95C22F9EE090A74ED92520407C195609A9F8134257F89E572E1751C2FB63C70` ; les deux captures finales ont été ouvertes et examinées : contenu complet, champs et actions accessibles, provenance/disponibilité lisibles, avertissement partiel distinct, sans clipping ni débordement
- Critères : parallélisme et éligibilité prouvés par les tests différés et les spies ; provenance, connexion, métadonnées et disponibilité prouvées par contrats, API et E2E ; déduplication sans perte et non-fusion sur le titre prouvées en unité ; panne partielle et timeout prouvés en unité, avec état partiel inspecté dans le navigateur ; entrées, quota de page et curseur bornés par Zod ; absence de secrets prouvée par sérialisation, scans, API réelle et bundle ; les cinq familles demandées — zéro connexion, doublons, panne partielle, timeout et concurrence — sont vertes
- Résultat : `PASS`
- Notes : une première passe PostgreSQL a échoué uniquement parce que l'assertion `toMatchObject` décrivait un tableau d'un élément alors que la borne demandée en retournait correctement deux ; l'assertion a été rendue explicite puis le harnais a passé. Les derniers parcours E2E reconstruisent l'image API après le durcissement final des messages fournisseur. `QUEUE-001` devient le prochain item `READY`.

## V-010 — QUEUE-001, file collaborative temps réel

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; Socket.IO serveur/client 4.8.3 ; images Node.js 24 Alpine et nginx 1.28 Alpine
- Périmètre : contrats Zod de file, `QueueService` transactionnel, reçus idempotents, positions monotones, routes snapshot/ajout/retrait/ordre, événements Socket.IO versionnés, resynchronisation et polling dégradé, interface mobile/desktop, proxy WebSocket nginx, tests et décision D-024 ; aucune nouvelle migration
- Données : résultats du fournisseur fake rattachés au lobby ; schémas PostgreSQL et projets Compose jetables `easyplaylist-pg-tests-21936`, `easyplaylist-e2e-26088` et prédécesseurs de validation, tous arrêtés avec suppression de leurs volumes par les harnais ; aucun compte ou credential fournisseur réel
- Commandes finales : `npm.cmd run format:check` (code `0`) ; `npm.cmd run lint` (code `0`, aucun warning) ; `npm.cmd run typecheck` (code `0`) ; `npm.cmd run test` (9 fichiers, 53 tests, code `0`) ; `npm.cmd run test:failure-probe:verify` (code rouge interne attendu `1`, wrapper `0`) ; `npm.cmd run test:postgres` (4 fichiers, 5 tests, code `0`) ; `npm.cmd run build` (code `0`) ; `docker compose config` et `docker compose build` (codes `0`) ; `npm.cmd run e2e` (1 parcours, code `0`) ; scans `rg` des champs de credential dans le code de file, le web et `apps/web/dist` (aucun résultat, code `1` attendu)
- Domaine/PostgreSQL : le test d'intégration ajoute deux commandes concurrentes sous verrou et observe les versions successives ; rejoue un même identifiant sans seconde ligne ; refuse sa réutilisation avec un autre contenu ; refuse un titre rattaché à un autre lobby ; fait gagner exactement un de deux ordres portant la même version et retourne à l'autre `QUEUE_VERSION_CONFLICT` avec le snapshot versionné ; retire puis réordonne à nouveau pour couvrir la présence d'une ligne historique retirée ; un participant du lobby B ne peut pas lire la file A
- API/temps réel réel : la stack Docker accepte une commande d'ajout puis son rejeu avec `replayed: true` et un seul titre, avant retrait de la fixture ; deux `PUT /queue/order` concurrents avec la même version produisent exactement `200` et `409`, le conflit contenant la version autoritaire suivante ; une identité étrangère reçoit `404 LOBBY_NOT_FOUND` sur `GET /queue` ; les rooms Socket.IO sont rejointes après réauthentification du cookie et les mises à jour ne sont publiées qu'après la réponse durable
- Navigateur : Camille et Noor ajoutent chacun un titre, observent le même ordre et les auteurs, Noor réordonne, Camille retire ; une coupure réseau force l'état « Temps réel dégradé », Camille ajoute pendant la panne, puis Noor revient en « Temps réel connecté » et reçoit le snapshot complet avec le titre manqué ; un rafraîchissement conserve la file et un troisième membre Alex rejoint ensuite avec les deux mêmes titres ; les contextes contrôlent absence d'erreur console inattendue et de débordement horizontal
- Logs : la passe finale contient les statuts attendus `201`, `200`, `409` et `404`, les upgrades WebSocket `101` et des `requestId` Fastify ; aucune exception applicative, erreur PostgreSQL ou credential n'apparaît ; seuls les avertissements `locale` et authentification locale de l'image PostgreSQL jetable restent présents. Une passe intermédiaire a révélé `23505` lors d'un ordre après retrait ; les positions ont été rendues monotones par version, un test de régression a été ajouté, puis PostgreSQL et E2E ont été rejoués avec succès.
- Captures : `artifacts/validation/queue-001-collaborative-mobile.png`, 390 px de large, SHA-256 `C74AAA03669A96A21A227B876EE7444BA03F2D1C408C4065CF12B49432E89D50` ; `artifacts/validation/lobby-001-lobby-desktop.png`, 1280 × 900, SHA-256 `65E1C4927FE34E8D5C150BE16214DBD3BCC9911725D456DF4EBE0408C2AC1CBE` ; toutes deux ouvertes et examinées après la passe finale : titres, auteurs, ordre, commandes, version et état temps réel lisibles, contraste cohérent, aucun clipping ni débordement
- Critères : ajout membre et résultat autorisé prouvés par contrats, API réelle et PostgreSQL ; snapshots et versions identiques prouvés par deux membres plus un observateur ; rejeu sans doublon prouvé dans l'intégration et sur la stack réelle ; rupture et resynchronisation complètes prouvées par la coupure/reprise ; retrait et réordonnancement ouverts aux membres prouvés dans l'UI, avec régression après retrait ; conflits concurrents déterministes et observables prouvés par un gagnant `200` et un perdant `409` portant le snapshot ; reconnexion, doublon, conflit d'ordre, isolation inter-lobby et panne WebSocket sont tous couverts
- Résultat : `PASS`
- Notes : Socket.IO réalise la dépendance déjà prévue par D-018 et reste mono-instance, conformément au MVP sans cache distribué. Aucun fake ne sert de preuve Spotify. `PLAYER-001` devient le prochain item `READY` et constitue le prochain jalon de validation utilisateur prévu par le processus.

## V-011 — PLAYER-001, bail lecteur et playback fake

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; Socket.IO serveur/client 4.8.3 ; images Node.js 24 Alpine et nginx 1.28 Alpine
- Périmètre : contrats Zod de lecture, migration `003_playback_coordination.sql`, `PlaybackService` transactionnel, bail avec génération, contrôles et rapports idempotents, fake isolé par lobby, routes HTTP, signal temps réel personnalisé, interface mobile/desktop, tests et décision D-025 ; aucune dépendance ajoutée
- Données : quatre titres fake, trois participants et deux lobbies dans des schémas PostgreSQL `test_<uuid>` ; projets Compose jetables `easyplaylist-pg-tests-16488`, `easyplaylist-e2e-5656` et prédécesseurs de validation ; tous les conteneurs, réseaux et volumes ont été supprimés par les orchestrateurs
- Commandes finales : `npm.cmd run format:check` (code `0`) ; `npm.cmd run lint` (code `0`, aucun warning) ; `npm.cmd run typecheck` (code `0`) ; `npm.cmd test` (9 fichiers, 55 tests, code `0`) ; `npm.cmd run test:failure-probe:verify` (code rouge interne attendu `1`, wrapper `0`) ; `npm.cmd run test:postgres` (5 fichiers, 6 tests, code `0`) ; `npm.cmd run build` (code `0`) ; `docker compose config --quiet` (code `0`) ; `npm.cmd run e2e` (1 parcours, code `0`) ; scan `rg --pcre2` de clés privées et tokens hors dépendances/docs/tests (aucun résultat, code `1` attendu)
- PostgreSQL/migrations : `001`, `002` et `003` passent sur schéma vide ; le chemin représentatif `001 → 002 → 003` conserve le lobby ; `003` ajoute l'identifiant interne de commande fournisseur et une dernière transition JSON contrainte. Le test lecteur verrouille le lobby, refuse un participant d'un autre lobby, garde un seul bail, prolonge l'expiration par heartbeat puis autorise une reprise après six secondes.
- Domaine/idempotence : Camille réclame le bail et Noor reçoit `LEASE_HELD`; Noor peut néanmoins démarrer, mettre en pause et passer, puis Camille reprendre et signaler fin/échec. Les quatre transitions successives montrent `Premier → Deuxième → Troisième → Quatrième`; les titres terminés/quittés sortent de la file active, l'échec reste public comme dernière transition, et le rejeu de `start` retourne `replayed: true` sans nouvelle mutation. Un rapport portant le mauvais navigateur est refusé par `LEASE_LOST`.
- API/temps réel réel : la stack Docker expose les cinq routes player/playback validées aux frontières ; un deuxième contexte reçoit `409 LEASE_HELD`; trois contextes voient le même titre courant, la fin et l'échec ; `playback:event` ne transporte que lobby et version, chaque client relisant son snapshot personnalisé. Les changements de titre publient aussi la nouvelle file après commit. La fermeture du premier navigateur arrête son heartbeat ; l'observateur voit le bail disponible puis le réclame avec succès.
- Navigateur/accessibilité : Camille, Noor et Alex couvrent ajout, lecture commandée par un non-détenteur, impossibilité de produire l'audio sur le second navigateur, fin, échec, perte et reprise. Les contrôles portent des noms accessibles, le statut du bail et le titre courant utilisent des régions lisibles, la coupure/reconnexion explicite reste fonctionnelle, les viewports 390 px et 1280 px n'ont aucun débordement horizontal et la console finale est vide.
- Logs/confidentialité : les requêtes player/playback portent `requestId` et les statuts attendus `200`, `409` et WebSocket `101`; aucune exception applicative, erreur PostgreSQL, credential ou identifiant de navigateur n'apparaît dans les événements publics ou le bundle. Seuls les avertissements `locale` et authentification locale de PostgreSQL Alpine jetable sont présents.
- Captures : `artifacts/validation/player-001-playing-mobile.png`, 390 × 2557, SHA-256 `0DD65635EA36F84A90207EAF0E106DF6A6E47C3CFD54D59DA6CAEE3AA6A514BE` ; `artifacts/validation/player-001-lease-recovery-mobile.png`, 390 × 1873, SHA-256 `D1F029CA0D8F05F81FC112CB2AB97534BC9341988ABD217677C1EF4A493B53CE` ; `artifacts/validation/lobby-001-lobby-desktop.png`, 1280 × 1552, SHA-256 `BB9A0FCB60F8DF090BBE2DB5C3985C60D8300932CBCFFC28B159A2907FE16860`. Les trois ont été ouvertes et examinées : titre courant et état « En lecture », détenteur unique, commandes fake, transition d'échec, reprise sur Alex, hiérarchie et contrastes lisibles, sans clipping.
- Critères : bail exclusif, heartbeat et expiration prouvés par PostgreSQL et trois navigateurs ; second navigateur refusé mais commandes membres acceptées par API/E2E ; délai de six secondes documenté et reprise observée ; démarrage, fin, passage, échec, pause et reprise couverts en intégration, avec fin/échec dans le navigateur ; titre/état synchronisés par événement puis snapshot ; rejeu idempotent prouvé ; l'E2E à trois contextes couvre toute la séquence ajout, lecture, perte et reprise.
- Résultat : `PASS`
- Notes : les deux premières extensions E2E ont révélé que l'événement navigateur `offline` n'entraînait pas immédiatement l'état dégradé, puis que le retour `online` ne forçait pas la reconnexion Socket.IO. Le client déconnecte/reconnecte désormais explicitement le socket ; PostgreSQL, statique et E2E ont tous été rejoués avec succès. Le premier appel Vitest de la tranche utilisait par erreur l'option Jest `--runInBand` et n'a exécuté aucun test ; les scripts déclarés ont ensuite servi à toutes les passes finales. `PROVIDER-002` passe `BLOCKED` tant qu'aucun compte Spotify Premium allowlisté ni credentials de test ne sont fournis ; `LOBBY-002`, dont les dépendances sont terminées, devient le prochain item `READY`.

## V-012 — LOBBY-002, reconnexion, expiration et fermeture/purge

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; Socket.IO serveur/client 4.8.3 ; images Node.js 24 Alpine et nginx 1.28 Alpine
- Périmètre : contrats Zod de fermeture et événement `lobby.closed`, `LobbyLifecycleService`, fermeture créateur, arbitrage fermeture/action, révocation fake et purge des connexions, expiration bornée et rejouable, configuration de durée et de lot, écran final mobile, tests, architecture et D-026 ; aucune nouvelle dépendance ni migration, les colonnes de cycle de vie et cascades existaient depuis `001`
- Données : identités invitées et lobbies dans des schémas `test_<uuid>` ; une enveloppe chiffrée factice structurellement valide pour prouver sa suppression ; projets Compose jetables `easyplaylist-pg-tests-12644`, `easyplaylist-e2e-8004` et passes de correction, tous arrêtés avec suppression des volumes, réseaux et conteneurs par les harnais ; aucun credential réel
- Commandes finales : `npm.cmd run format:check` (code `0`) ; `npm.cmd run lint` (code `0`, aucun warning) ; `npm.cmd run typecheck` (code `0`) ; `npm.cmd run test` (9 fichiers, 56 tests, code `0`) ; `npm.cmd run test:failure-probe:verify` (code rouge interne attendu `1`, wrapper `0`) ; `npm.cmd run test:postgres` (6 fichiers, 8 tests, code `0`) ; `npm.cmd run build` (code `0`) ; `docker compose config` et `docker compose build` (codes `0`) ; `npm.cmd run e2e` (1 parcours, code `0`) ; scan `rg` des champs sensibles dans `artifacts/validation` (aucun résultat, code `1` attendu)
- PostgreSQL/migrations : la suite applique `001`, `002` et `003` sur schéma vide puis couvre encore le chemin représentatif `001 → 002 → 003`. Une reconnexion avec le même cookie conserve deux memberships au total et met à jour le pseudonyme sans doublon. L'invité reçoit `403 LOBBY_CREATOR_REQUIRED`; la fermeture créateur et un ajout de file partent sur deux connexions PostgreSQL distinctes, produisent fermeture `200` et action `404` dans la passe finale, puis toute action et jonction ultérieure reçoit `404`. Statut `closed`, zéro bail et zéro connexion fournisseur sont vérifiés directement.
- Expiration/purge : un lobby ouvert échu avec une connexion chiffrée passe à `expired` dans un lot de taille `1`, supprime exactement une connexion et retourne son identifiant ; le second passage retourne zéro ligne et zéro suppression. Une borne nulle est refusée. La fermeture tente `ProviderCatalog.purgeLobby`, révoque le fake capable, puis la clause `finally` supprime les credentials persistés ; D-026 documente la reprise des fermetures interrompues par le balayage.
- API/temps réel réel : sur la stack Docker, un invité reçoit `403` sur `DELETE /lobbies/:id`, le créateur reçoit `200`, `lobby.closed` fait basculer l'observateur sans rechargement et un ajout postérieur reçoit `404`. La reprise de l'identité, de la file et du titre courant, le rejeu d'ajout sans doublon, la perte du heartbeat et la reprise du bail restent exercés dans le même parcours à trois contextes. Les routes de retrait et de réordonnancement par membres restent couvertes avant fermeture.
- Navigateur/accessibilité : l'action destructive n'est rendue que pour le créateur, porte un libellé accessible et exige une confirmation native. Le créateur et l'observateur voient l'écran « La soirée est terminée » après le commit. Le parcours contrôle la console, les statuts temps réel et l'absence de débordement horizontal ; aucune expulsion de participant n'est retenue dans le MVP, donc le critère conditionnel associé ne s'applique pas.
- Logs/confidentialité : la passe finale montre les healthchecks `200`, WebSocket `101`, fermeture `200`, refus invité `403` et action après fermeture `404`, sans exception applicative ni erreur PostgreSQL. Les champs de credential ne sont pas sérialisés dans les événements, réponses ou captures ; seuls les avertissements connus de locale et d'authentification locale de PostgreSQL Alpine jetable apparaissent.
- Captures : `artifacts/validation/lobby-002-closed-mobile.png`, 390 × 844, SHA-256 `EC66059CAD20A6F3539E9521D0C5D9269E200B396F110A3337B924439656567E` ; `artifacts/validation/lobby-001-lobby-desktop.png`, 1280 × 1653, SHA-256 `B358E251DFC01332D76C9CA190CFCCE9A55068306D0AE8A512282E5CF82B0D5B`. Les deux ont été ouvertes et examinées après la passe finale : action « Fermer le lobby » distincte, confirmation expliquée, écran final lisible, focus et contrastes cohérents, aucun clipping ni débordement.
- Critères : droits ordinaires de retrait/réordre et fermeture créateur prouvés par E2E/API ; reconnexion sans duplication prouvée par cookie, compte de memberships, snapshots et trois navigateurs ; perte/reprise propre du bail prouvée dans le même parcours et suppression du bail à la fermeture en PostgreSQL ; fermeture, refus des actions, révocation capable et suppression des secrets prouvés ; expiration 24 heures par défaut, lot borné et double purge inoffensive prouvés ; course fermeture/action exercée sur connexions distinctes ; aucune expulsion n'étant incluse, son test conditionnel est sans objet
- Résultat : `PASS`
- Notes : une première passe PostgreSQL a utilisé par erreur un code contenant `L`, caractère interdit, puis a été corrigée. La première assertion visuelle cherchait une chaîne mojibake alors que le nouvel écran emploie l'UTF-8 correct ; la preuve a été relancée. Une capture d'abord nommée mobile héritait du viewport desktop ; le test fixe désormais explicitement `390 × 844` et la passe finale a régénéré puis fait examiner la bonne image. `LOBBY-002` est `DONE`. Aucun item n'est désormais `READY` : `PROVIDER-002` reste `BLOCKED` par l'absence de compte Premium allowlisté et de credentials Spotify de test, donc `SEC-001` conserve son statut `TODO`.

## V-013 — PROVIDER-003, intégration YouTube configurable

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; images Node.js 24 Alpine et nginx 1.28 Alpine
- Périmètre : décision D-027, adaptateur `youtube`, configuration serveur, recherche Data API simulée, sélection de variante, coordination du playback, composant IFrame visible, état fournisseur, erreurs bornées, documentation et tests ; aucune migration ni dépendance ajoutée
- Configuration : `YOUTUBE_API_KEY` optionnelle et exclusivement serveur ; `YOUTUBE_REGION_CODE=FR` par défaut ; aucune clé Google présente dans l'environnement ou les fichiers locaux au moment de la validation. Sans clé, YouTube déclare lecture web/pause/passage mais un statut `unavailable` pour la recherche, et le catalogue fake reste actif.
- Commandes finales : `npm.cmd run format:check`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`, `docker compose config --quiet`, `npm.cmd run test:postgres`, `npm.cmd run e2e` et scans de secrets/bundle ; tous les codes finaux sont `0`. Les harnais Docker ont supprimé leurs conteneurs, réseaux et volumes jetables.
- Adaptateur : les tests injectent les réponses de `search.list` et `videos.list`, exigent `type=video`, catégorie musique, intégrabilité, syndication et région, transforment titre HTML, chaîne, miniature et durée ISO 8601, écartent une vidéo non intégrable, bornent un refus `403/429`, refusent une configuration absente sans réseau et exercent démarrage, pause, reprise et passage sans prétendre diffuser côté serveur.
- API et confidentialité : `ProviderCatalog` sélectionne YouTube pour la recherche lorsque la clé existe, sinon le fake ; un identifiant vidéo doit respecter le format public YouTube et une variante jouable autorisée. La clé ne fait partie d'aucun schéma public, événement ou prop React. Le scan de motifs de clé privée, JWT et clé Google est vide ; le bundle web ne contient ni `YOUTUBE_API_KEY`, ni clé de test, ni champs de credential.
- Lecteur navigateur : seul le navigateur dont le snapshot porte `heldByCurrentDevice` instancie `YT.Player`. La zone reste visible, mesure au moins 200 px de haut, conserve les contrôles officiels, suit `playing`/`paused`, remonte `ended` et erreur avec génération de bail, déduplique les rapports et propose « Activer la lecture » après blocage d'autoplay. L'interface explique explicitement l'absence d'audio isolé ou de lecture en arrière-plan.
- PostgreSQL/Docker/E2E : les migrations existantes passent sur base isolée, les tests d'intégration PostgreSQL restent verts, les images API/web se reconstruisent, les trois healthchecks deviennent sains et le parcours mobile complet à trois contextes passe sans erreur console inattendue. L'E2E valide l'état YouTube non configuré et le maintien du parcours fake ; il ne constitue pas une preuve YouTube réelle.
- Capture : `artifacts/validation/provider-001-fake-mobile.png`, 390 × 2222, SHA-256 `C175517A653F51224718DA449778A391870AF8A88CB359E2DC1B0F74F11A0F9B`. Capture ouverte et examinée : carte YouTube « À configurer », capacités et limites lisibles, carte fake distincte, hiérarchie mobile cohérente, aucun clipping horizontal.
- Critères prouvés : configuration optionnelle et secret serveur ; mapping et filtrage recherche sur réponses officielles simulées ; ajout/lecture autorisés par les contrats communs ; création IFrame limitée au détenteur ; commandes/rapports coordonnés ; états de configuration, quota, intégrabilité et autoplay couverts par code/tests/interface ; documentation produit et architecture alignée.
- Preuve manquante : aucun projet Google Cloud ni clé réelle n'est disponible. La recherche contre YouTube Data API v3, l'intégration d'un résultat réel, le chargement effectif de l'IFrame, la fin d'une vidéo et les contraintes de territoire/autoplay sur appareils réels n'ont donc pas été exercés. Une simulation ne peut pas satisfaire ce critère externe.
- Résultat : `VERIFY`
- Suite : créer une clé limitée à YouTube Data API v3, la placer uniquement dans `.env`, reconstruire l'API, puis exécuter le script manuel avec une vidéo intégrable sur navigateur lecteur et téléphone. Si recherche, lecture, fin, erreur et absence de secret sont prouvées, ajouter une validation réelle et passer `PROVIDER-003` à `DONE`.

## V-014 — PROVIDER-003, preuve réelle YouTube

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; région YouTube `FR`
- Périmètre : clé Google locale chargée exclusivement par l'API, recherche YouTube Data API v3 réelle, ajout de résultats, bail lecteur, IFrame Player officiel visible, lecture audible, pause, reprise, erreur d'intégration réelle, confidentialité, harnais reproductible et non-régression complète
- Configuration : `.env` contient une unique `YOUTUBE_API_KEY` non vide au format attendu et reste ignoré par Git. La valeur n'a été imprimée, copiée dans aucun artefact ni transmise au navigateur. L'API reconstruite expose YouTube avec l'état `active` et les capacités `catalog_search`, `track_metadata`, `web_playback`, `pause_resume` et `queue_control`.
- Recherche/API réelle : un lobby invité sans compte YouTube a recherché `Daft Punk Around the World`, reçu cinq résultats YouTube réels et ajouté des variantes autorisées à la file. Une seconde recherche `NoCopyrightSounds music` a retourné dix résultats réels ; cinq ont été ajoutés sans OAuth participant.
- Lecture réelle : `npm.cmd run validate:youtube-real` a créé un lobby, réclamé le bail puis démarré `NCS: Feels Like Summer Mix | NCS - Copyright Free Music` dans l'IFrame visible. La vidéo a avancé de `3,02 s`, avec `muted=false`, `paused=false`, `volume=0,5902` et une durée annoncée de `2554,061 s`. Le script a ensuite prouvé pause puis reprise par observation du média dans l'IFrame. Aucune erreur console EasyPlaylist ni alerte YouTube n'a été observée sur ce parcours.
- Erreur réelle bornée : cinq vidéos retournées pour la recherche Daft Punk ont produit le code IFrame `150`, refus d'intégration décidé par leurs ayants droit. Chaque erreur a été rapportée au serveur comme `failed` et la file est passée automatiquement au titre suivant sans crash ; après cinq refus, elle s'est terminée proprement. Cette preuve confirme que le filtre Data API réduit les résultats incompatibles sans pouvoir remplacer l'autorité d'exécution territoriale de l'IFrame.
- Isolation du quota : `scripts/run-compose-e2e.mjs` force désormais `YOUTUBE_API_KEY` à une chaîne vide. La suite déterministe ne lit donc jamais la clé du `.env` Compose et ne consomme pas le quota Google ; seul le script explicite `npm run validate:youtube-real` effectue la preuve externe.
- Commandes finales : `npm.cmd run validate:youtube-real`, `npm.cmd run format:check`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`, `docker compose config --quiet`, `npm.cmd run test:postgres` et `npm.cmd run e2e` ; tous les codes finaux sont `0`. Résultats : 67 tests unitaires dans 10 fichiers, 8 tests PostgreSQL dans 6 fichiers et 1 parcours Playwright Docker complet.
- Docker : les services produit `db`, `api` et `web` sont `healthy`; l'API répond `ready` sur `127.0.0.1:3000` et l'interface reste disponible sur `127.0.0.1:5173`. Les projets PostgreSQL et E2E jetables ont supprimé leurs conteneurs, réseaux et volumes.
- Confidentialité : le scan exact de la valeur locale trouve zéro correspondance hors `.env`; les logs Compose n'en contiennent aucune ; le scan générique `AIza…` trouve zéro fichier dans `apps/web/dist`. Le fichier temporaire contenant le cookie du smoke API a été supprimé.
- Captures : `artifacts/validation/provider-003-youtube-real-player.png`, 697966 octets, SHA-256 `BD00718E41B889BF4D01EA9D25EC2EADF9F55F2CA1EC9F36DA9A94191C3835AC`, ouverte et examinée : source YouTube réelle, IFrame et contrôles officiels visibles, titre en lecture, file et recherche lisibles sans clipping. `artifacts/validation/provider-003-youtube-real-failure.png` conserve le parcours d'échec `150` sans secret.
- Critères : les sept critères de `PROVIDER-003` sont couverts par V-013 pour les contrats, tests simulés, erreurs et comportement sans clé, puis par cette preuve réelle séparée pour la recherche externe, l'ajout sans compte, le bail, l'IFrame visible, la lecture audible, la coordination et l'échec fournisseur réel.
- Résultat : `PASS`
- Suite : `PROVIDER-003` passe à `DONE`. Ses dépendances étant satisfaites avec `LOBBY-002`, `SEC-001` devient `READY`.

## V-015 — UX-001, hiérarchie du lobby

- Date : 2026-08-08
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; viewports 390 × 844 et 1280 × 900
- Périmètre : structure et styles React de l'écran actif, panneau natif de réglages, invitation, fermeture créateur, ordre lecteur/file/recherche, confirmation d'ajout, tests E2E, preuve YouTube réelle adaptée, documentation produit, D-028 et backlog ; aucun contrat HTTP, événement, schéma, migration ou dépendance modifié
- Hiérarchie : le test inspecte les enfants directs du lobby et prouve l'ordre `player-panel`, `queue-panel`, `search-panel` sur mobile. Le même ordre est visible sur la capture bureau. Le nom et le nombre de membres restent en tête, avec une taille de titre réduite et moins d'espace vertical.
- Réglages : le code, l'URL et la fermeture ne sont pas visibles lorsque le composant `<details>` est fermé. Le parcours ouvre son `<summary>` focalisable, lit un code valide, vérifie l'URL d'invitation, copie le lien et observe « Invitation copiée ✓ ». La fermeture est absente pour l'invité, cachée initialement pour le créateur, puis visible et exercée après ouverture avec confirmation native.
- Épuration : les textes et cartes « Source musicale » et « Mode démo » sont absents du DOM du lobby. Les routes fournisseur restent disponibles et autorisées côté API ; erreurs de recherche, état du bail, temps réel dégradé, transition de lecture et contraintes YouTube restent présentés à l'endroit où ils affectent l'action.
- Ajout : après mutation `201`, le résultat concerné affiche « Ajouté ✓ » pendant quatre secondes et une bannière `role=status` nomme le titre ajouté. Le test vérifie cette annonce, l'état du bouton et la réception temps réel du titre par un autre navigateur.
- Commandes finales : `npm.cmd run format:check`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`, `docker compose config --quiet`, `npm.cmd run test:postgres`, `npm.cmd run e2e`, `docker compose up -d --build --force-recreate --wait web` et `npm.cmd run validate:youtube-real` ; tous les codes finaux sont `0`. Résultats : 67 tests unitaires dans 10 fichiers, 8 tests PostgreSQL dans 6 fichiers et 1 parcours Playwright Docker complet.
- Régression réelle YouTube : la recherche réelle a ajouté cinq résultats. Le premier a été refusé par l'IFrame avec le code borné `150`, puis la file a avancé vers `NCS: Feels Like Summer Mix | NCS - Copyright Free Music`. La vidéo a progressé de `3,01 s`, avec `muted=false`, `volume=0,5902`, pause et reprise réussies, sans erreur console EasyPlaylist.
- Docker et confidentialité : `db`, `api` et `web` sont `healthy`; l'API répond `ready` et le web reconstruit est disponible sur `127.0.0.1:5173`. Zéro correspondance de la clé locale hors `.env`, zéro motif de clé Google dans le bundle, aucune valeur exacte dans les logs et aucune erreur Docker inattendue.
- Captures examinées : `artifacts/validation/ux-001-lobby-mobile.png`, 147678 octets, SHA-256 `04274D57B53B3757FF7124BDACB2ED7C70311185F757EC1E297088535FC54B8B`, montre lecteur, file, recherche basse et confirmation d'ajout sans débordement ; `artifacts/validation/ux-001-settings-mobile.png`, 163748 octets, SHA-256 `C3197A7FA89B4A8A92E1F4F6D8E4A5FEA20A4F162D7202A3445B84F984E77B79`, montre code compact, URL secondaire et fermeture dans les réglages ; `artifacts/validation/ux-001-lobby-desktop.png`, 240231 octets, SHA-256 `58569A189C381BE92E8297EEBAD95ABA4928F6892A6D2BC9BA77ECDC889D75E7`, confirme la hiérarchie bureau ; `artifacts/validation/provider-003-youtube-real-player.png`, 905329 octets, SHA-256 `377054608F53AB631CA43C7F2F0F75B43800043504765D6DFF905545387AD5F6`, confirme le lecteur YouTube visible au sommet de la nouvelle interface.
- Accessibilité et états : focus visible étendu au `<summary>`, libellés et boutons conservés, confirmation annoncée, absence de débordement horizontal vérifiée sur quatre pages, temps réel hors ligne puis reconnecté exercé. Les seules erreurs console ignorées sont les `ERR_INTERNET_DISCONNECTED` produites pendant la coupure réseau volontaire ; toute autre erreur reste bloquante.
- Notes : la première passe E2E ciblait le texte exact du résumé sans son pictogramme ; le sélecteur a été ancré sur `<summary>`. Une assertion historique supposait le premier titre d'une fixture de recherche asynchrone ; elle utilise désormais le titre réellement rendu. Chrome a également journalisé la coupure réseau volontaire ; le filtre final ignore uniquement son code explicite.
- Critères : les six critères de `UX-001` sont prouvés par le parcours, les trois captures UX inspectées et la régression réelle YouTube.
- Résultat : `PASS`
- Suite : `UX-001` passe à `DONE`. `SEC-001` reste le prochain item `READY` ; `QA-001` attend encore sa clôture.

## V-016 — PLAYER-002, silence des appareils non lecteurs

- Date : 2026-08-09
- Version/commit : working tree initial, aucun commit applicatif
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; région YouTube `FR` ; viewport téléphone 390 × 844 et lecteur réel 1280 × 900
- Périmètre : identité locale du navigateur lecteur, condition de création de l'IFrame YouTube, extinction du média à la perte du bail, tests unitaires et E2E, documentation produit et architecture, D-029 et backlog ; aucun contrat HTTP/temps réel, schéma partagé, migration, dépendance ou capacité fournisseur modifié
- Cause : l'identifiant du lecteur était conservé dans `localStorage`. Deux onglets partageant le même participant et la même origine réutilisaient donc le même UUID ; le snapshot personnalisé pouvait marquer les deux comme `heldByCurrentDevice`, ce qui permettait à l'onglet arrivant d'instancier un second lecteur local.
- Correctif : `getPlayerDeviceId` utilise désormais `sessionStorage`, valide toute valeur conservée avec `PlayerDeviceIdSchema` et crée un UUID distinct par onglet. Le même onglet conserve cet UUID au rechargement. Le démontage du composant YouTube exécute dans l'ordre `pauseVideo`, `mute`, puis `destroy`, et ignore une fin reçue après désactivation.
- Tests ciblés : `npm.cmd run test -- apps/web/src/player-device.test.ts apps/web/src/YoutubePlayer.test.ts` retourne `4/4`. Ils prouvent la stabilité dans une session d'onglet, la séparation de deux sessions, le remplacement d'un UUID invalide et l'ordre d'extinction `pause → mute → destroy`.
- Parcours navigateur : `npm.cmd run e2e` reconstruit une stack Docker jetable et passe `1/1`. Pendant la lecture, l'invité d'un second contexte et un nouvel onglet mobile du contexte créateur voient le titre courant et « Diffusé par Camille », n'ont ni contrôle de simulation ni lecteur local, et le second onglet possède un UUID `sessionStorage` différent. Une réclamation concurrente reste refusée avec `409 LEASE_HELD`; heartbeat, reconnexion, expiration/reprise du bail, commandes collaboratives et fermeture restent exercés sans erreur console inattendue.
- Régression YouTube réelle : après `docker compose up -d --build --force-recreate --wait web`, `npm.cmd run validate:youtube-real` ajoute cinq résultats réels et démarre `NCS: Feels Like Summer Mix | NCS - Copyright Free Music` uniquement sur le détenteur. La vidéo avance de `3,02 s` avec `muted=false`, `paused=false`, `volume=0,5902`, puis pause et reprise réussissent. Le premier résultat retourne le refus d'intégration réel et borné `150`, la file progresse, et `consoleErrors` reste vide.
- Validation complète : `npm.cmd run format:check`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run test:failure-probe:verify`, `npm.cmd run build`, `docker compose config --quiet`, `npm.cmd run test:postgres`, `npm.cmd run e2e`, reconstruction Compose et validation YouTube réelle retournent finalement `0`. Résultats : 71 tests rapides dans 12 fichiers, 8 tests PostgreSQL dans 6 fichiers, 1 parcours Playwright Docker et la sonde volontairement rouge correctement détectée.
- Docker/API/logs : les services produit `db`, `api` et `web` sont `healthy`; liveness et readiness retournent séparément `200`. Les migrations existantes passent sur le volume PostgreSQL jetable vierge, puis celui-ci est supprimé. Les logs du parcours montrent healthchecks, WebSocket `101`, conflit de bail attendu, heartbeat et commandes `200`, sans exception applicative ; le volume produit existant n'a pas été supprimé.
- Confidentialité : zéro correspondance exacte de la clé YouTube locale hors `.env` et zéro motif générique de clé Google dans le bundle, les artefacts, les sources ou la documentation. Aucun token, cookie ou credential n'est ajouté au contrat ou à l'état client.
- Captures examinées : `artifacts/validation/player-002-silent-second-tab-mobile.png`, 390 × 1310, 139023 octets, SHA-256 `3A474DDF556822CA93C764F1766F71CA4B8789AD5D12375A13BAF5CDB201CBB0`, montre le téléphone non lecteur avec le titre et « Diffusé par Camille », sans IFrame ni simulation et sans débordement ; `artifacts/validation/provider-003-youtube-real-player.png`, 1265 × 3019, 907628 octets, SHA-256 `E732E69638E41B8D78E2969F8DC3D3A760A1B4457F0DF1D3BEC8B4E8C3C69037`, montre le seul détenteur avec l'IFrame YouTube visible, ses contrôles, la file et la recherche sans clipping.
- Notes : la première passe complète a détecté une extension ESM manquante dans le nouveau test ; elle a été corrigée avant les passes finales. Docker Desktop était initialement arrêté ; après son démarrage, les stacks jetables et produit ont toutes atteint leurs healthchecks. La preuve YouTube finale a été répétée après reconstruction explicite de l'image web pour garantir qu'elle exécutait le correctif courant.
- Critères : arrivée silencieuse d'un appareil et d'un onglet prouvée par E2E et capture ; séparation d'identité et survie au rechargement prouvées par tests et parcours ; extinction immédiate prouvée par test d'ordre ; rapports et heartbeat réservés au détenteur couverts par les tests de playback existants et le conflit réel ; multi-navigateurs, contrôles et YouTube réel couverts par les passes finales.
- Résultat : `PASS`
- Suite : `PLAYER-002` passe à `DONE`. `SEC-001` reste le prochain item `READY`; `QA-001` attend sa clôture.

## V-017 — DEPLOY-001, Compose privé derrière Caddy

- Date : 2026-08-09
- Version/commit : working tree initial, aucun commit ni déploiement distant
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; images Node.js 24 Alpine et nginx 1.28 Alpine
- Périmètre : exposition des services dans `compose.yaml`, overrides `compose.local.yaml`, `compose.test.yaml` et `compose.production.yaml`, variables documentées, topologie Caddy système, procédure VPS, retour arrière, D-030 et backlog ; aucun contrat HTTP/temps réel, migration, dépendance, route nginx ou état visuel modifié
- Topologie Compose : les configurations fusionnées et analysées en JSON prouvent que production publie uniquement `web` sur `127.0.0.1:5173`, avec `api` et `db` privés et le réseau `backend` interne. L'override local publie `5173`, `3000` et `5432` sur `127.0.0.1` et rend seulement alors le réseau backend non interne. L'override de test publie uniquement PostgreSQL en plus du frontend, sur loopback. Aucune liaison `0.0.0.0` n'est produite.
- Garde de production : avec des fixtures non secrètes injectées dans le processus de validation, `compose.production.yaml` force `COOKIE_SECURE=true`, conserve le backend interne et accepte la configuration. Lorsque `POSTGRES_PASSWORD`, `GUEST_COOKIE_SIGNING_KEY`, `SECRETS_ENCRYPTION_KEY_V1` et `YOUTUBE_API_KEY` sont vidés, `docker compose config --quiet` échoue comme attendu.
- Validation statique et rapide : `npm.cmd run format:check`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run test:failure-probe:verify` et `npm.cmd run build` retournent `0`. Résultats : 12 fichiers et 71 tests rapides ; la sonde volontairement rouge retourne `1` en interne et son wrapper `0`.
- PostgreSQL/migrations : `npm.cmd run test:postgres` démarre un projet Compose jetable sur base vierge, applique les migrations versionnées et passe 6 fichiers/8 tests d'intégration, puis supprime conteneur, réseau et volume. L'override de test rend PostgreSQL joignable sur loopback pendant ce seul parcours.
- Images, healthchecks et panne : `docker compose build` reconstruit les images API/web. La pile jetable `easyplaylist-deploy-verify`, avec l'override local et les ports `25174`, `23001` et `25434`, montre les trois services `healthy` et chaque port sur `127.0.0.1`. Le socket PostgreSQL est joignable, le web, la liveness API directe et la readiness relayée retournent `200`. Après `docker compose stop db`, la liveness relayée reste `200` et la readiness devient `503`. Logs inspectés, puis conteneurs, réseaux et volume exacts supprimés.
- API, temps réel et navigateur : `npm.cmd run e2e` construit une autre pile de base vierge sans port API ou PostgreSQL, attend ses trois healthchecks et passe le parcours Playwright complet `1/1`. Les logs montrent `/api/health/ready` en `200`, mutations API via nginx, connexions `/socket.io` en `101`, cas d'autorisation et fermeture attendus, sans exception applicative inattendue. Le parcours contrôle aussi les erreurs console. Aucune nouvelle capture n'est requise car l'interface n'a pas changé.
- Confidentialité : `.env` reste ignoré par Git ; le scan des fichiers touchés ne trouve aucune clé Google, clé privée ou JWT. Les valeurs utilisées pour valider les secrets obligatoires sont des fixtures explicitement non secrètes et n'ont pas été écrites dans le dépôt.
- Documentation : `Docs/architecture/deployment.md` fournit le DNS, le `.env` minimal, les commandes Compose de production, le Caddyfile à deux hôtes, validation/rechargement, healthchecks, logs et retour arrière sans `--volumes`. Le port GuessThePolitician existant n'est pas modifié depuis ce dépôt ; son resserrement futur vers loopback est seulement recommandé.
- Preuve externe manquante : le DNS `playlist.guesstheappliance.com`, le Caddyfile du VPS, son certificat TLS et les réponses HTTPS publiques n'ont pas été modifiés ni vérifiés depuis l'environnement local. Les Compose sont prêts, mais publier sur le VPS nécessite une action explicite de l'utilisateur.
- Résultat : `PARTIAL`
- Suite : déployer les fichiers sur `/opt/EasyPlaylist`, créer le DNS, exécuter le Compose avec `compose.production.yaml`, ajouter le bloc Caddy documenté, puis prouver les deux endpoints HTTPS. `DEPLOY-001` passe à `VERIFY` jusqu'à cette preuve distante.

## V-018 — DEPLOY-001, preuve HTTPS publique

- Date : 2026-08-09
- Version/commit : déploiement VPS réalisé par l'utilisateur à partir de la livraison qualifiée dans V-017 ; aucun changement de code pour cette validation complémentaire
- Environnement : contrôle externe depuis Windows/PowerShell vers `playlist.guesstheappliance.com`, déploiement public Hostinger derrière Caddy
- Confirmation utilisateur : l'application déployée fonctionne sur le VPS.
- DNS et TLS : `Resolve-DnsName -Name playlist.guesstheappliance.com -Type A` résout le sous-domaine vers l'adresse IPv4 du VPS. Les requêtes HTTPS aboutissent avec une chaîne TLS acceptée par le client.
- Frontend et santé : `Invoke-WebRequest` retourne `200` pour `/` avec un document HTML EasyPlaylist, `200` et `ok` pour `/healthz`, puis `200` et `{"checks":{"database":"up"},"service":"api","status":"ready"}` pour `/api/health/ready`.
- Temps réel : une ouverture Engine.IO publique sur `/socket.io/?EIO=4&transport=polling` retourne `200`, `text/plain` et un paquet d'ouverture `0{...}` ; le relais Caddy → nginx → API est donc joignable sur le chemin temps réel documenté.
- Exposition : des tentatives TCP externes bornées à trois secondes vers les ports `5173`, `3000` et `5432` échouent toutes. Le frontend, l'API et PostgreSQL ne sont pas publiés directement sur l'interface publique ; seuls les accès HTTPS via Caddy ont été observés.
- Couverture : V-017 prouve les configurations Compose, secrets obligatoires, images, migrations, healthchecks, panne de base, API, temps réel local et navigateur. V-018 apporte la preuve distante qui manquait : DNS, HTTPS, frontend, readiness PostgreSQL, relais Socket.IO et absence d'exposition des ports internes.
- Interface : aucune modification visuelle n'a été introduite depuis les captures et le parcours navigateur de V-017 ; aucune nouvelle capture n'est requise pour cette validation de déploiement.
- Résultat : `PASS`
- Suite : `DEPLOY-001` passe à `DONE`. `SEC-001` reste le prochain item `READY` et ne dépend pas de Spotify Premium ; `QA-001` reste `TODO` jusqu'à sa clôture.

## V-019 — BLIND-001, mode blind test

- Date : 2026-08-09
- Version/commit : working tree initial, aucun commit ni déploiement distant
- Environnement : Windows/PowerShell ; Node.js 22.19.0 ; npm 11.4.1 ; Docker 29.5.2 ; Docker Compose 5.1.3 ; Chrome 151 ; PostgreSQL 18.4 Alpine ; YouTube réel en région `FR` ; viewports 390 × 844 et 1280 × 900
- Périmètre : réglage persistant créateur, migration `004_blind_test_mode.sql`, DTO lobby versionné, route `PATCH /lobbies/:id/settings`, événement `lobby.settings.updated`, unions de snapshots file/lecteur expurgées, refus des mutations de file révélatrices, synchronisation client, interface responsive, tests, documentation, D-031 et sonde YouTube blind test ; aucune dépendance ajoutée
- Commandes finales : `npm.cmd run format:check`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run test` (12 fichiers, 78 tests), `npm.cmd run test:failure-probe:verify` (échec interne `1` attendu, wrapper `0`), `npm.cmd run test:postgres` (7 fichiers, 9 tests), `npm.cmd run build`, `docker compose config --quiet`, `npm.cmd run e2e` (2 parcours), `docker compose up -d --build --force-recreate --wait api web`, `npm.cmd run validate:youtube-blind-real`, healthchecks et scans de secrets ; tous les codes finaux attendus sont obtenus
- Migration : les migrations `001` à `004` passent sur un schéma vierge, une seconde exécution est vide et `blind_test_enabled` vaut `false` par défaut ; le chemin de mise à niveau depuis `001` conserve le lobby existant et ajoute le même défaut `false`. Le harnais PostgreSQL jetable `easyplaylist-pg-tests-24740` a supprimé son conteneur, son réseau et son volume après succès.
- Autorisation et cohérence : le créateur active et désactive le mode pendant une lecture active ; un participant reçoit `403`, une valeur inchangée reste un no-op et seul un changement incrémente la version et publie `lobby.settings.updated`. Réglage, ajouts et commandes prennent le verrou du lobby ; file et lecteur portent la version du lobby et le client rejette tout snapshot antérieur à la bascule.
- Confidentialité API/temps réel : la file aveugle expose seulement `lobbyId`, `version`, `generatedAt`, `blindTestEnabled` et `queuedCount`. Le retrait et le réordonnancement avec des identifiants obtenus avant activation retournent `409 BLIND_TEST_QUEUE_HIDDEN`, et leurs réponses ne réémettent pas ces identifiants. Le lecteur non détenteur expose état, bail et pseudonyme de l'auteur, sans titre, artiste, album, miniature, durée, variante ni identifiant fournisseur ; seul le détenteur reçoit `{ provider, providerTrackId }` pour le morceau courant. Les transitions aveugles restent génériques.
- Navigateur : le parcours Docker à trois contextes passe activation avec confirmation, effacement immédiat des titres déjà visibles, compteur synchronisé, recherche puis ajout avec formulaire vidé et message neutre, « Musique de Adil », lecteur factice seulement chez le détenteur, refus d'un ancien identifiant, reconnexion en mode aveugle, passage à la lecture et désactivation avec retour de la file détaillée. Les deux parcours Playwright passent en `21,7 s`, sans erreur console ni débordement horizontal ; les services et volumes E2E sont supprimés par le harnais.
- YouTube réel : la sonde blind test ajoute cinq résultats réels, masque ensuite leurs métadonnées, réclame le bail et affiche « Musique de Lecteur de validation ». Un premier résultat refuse l'intégration avec le code officiel borné `150`, puis le suivant démarre ; la vidéo avance de `3,02 s`, avec `muted=false`, `paused=false` et volume `0,5902`. Pause et reprise passent. L'IFrame officielle reste visible à `978,4 × 549,5 px`, donc au-dessus du minimum 200 × 200, et aucune erreur console applicative n'est observée.
- Docker et logs : les services locaux `db`, `api` et `web` reconstruits sont `healthy`; liveness et readiness relayées retournent `200`. Les logs montrent la migration sur le volume existant, les WebSocket `101`, la bascule `PATCH 200`, les snapshots de file passant de réponses détaillées à environ 143 octets, les commandes de lecture `200` et le rapport d'échec fournisseur suivi d'une progression, sans exception applicative ni secret.
- Confidentialité du dépôt : le scan de clé Google, clé privée et JWT ne trouve aucune correspondance dans les sources, tests, documentation, Compose ou artefacts. Le bundle web ne contient aucun des champs `accessToken`, `refreshToken`, `encryptedCredentials`, `ciphertext` ou `authTag`.
- Captures examinées : `artifacts/validation/blind-001-non-player-mobile.png`, 390 × 1322, SHA-256 `E5A25C6D0932DED920380852F7B5D562AD44105A6B7EB431309F9C0FD74BA35D`, montre Noor avec le bandeau, « Musique de Adil » et le compteur, sans IFrame ni métadonnée ; `artifacts/validation/blind-001-player-desktop.png`, 1280 × 1178, SHA-256 `EB40234EBD9F547965D9844E4CA0EC2E731B7B0B725CFF8354B92938E29D9C1B`, montre le détenteur factice et les réglages ; `artifacts/validation/blind-001-youtube-player-real.png`, 1280 × 1781, SHA-256 `C96BC09A42B3A9D92C6221F30A668DEA2D8C3F5D6BDC9BE18914104CCCB5BD13`, montre le détenteur réel avec l'IFrame YouTube visible, le titre uniquement dans le lecteur officiel, la file secrète et aucun clipping. Les trois captures ont été ouvertes et examinées.
- Critères : persistance et défaut prouvés par migration ; autorisation, no-op, version et événement par unité et PostgreSQL ; absence de fuite structurelle par contrats, API réelle, corps HTTP et parcours non lecteur ; anciens identifiants refusés pour retrait et ordre ; commandes collaboratives, reconnexion, passage automatique et retour normal par E2E ; lecteur YouTube réel visible et fonctionnel uniquement sur la branche détenteur ; accessibilité du switch, libellés neutres, mobile et bureau inspectés.
- Résultat : `PASS`
- Suite : `BLIND-001` passe à `DONE`. `SEC-001` devient le prochain item `READY` et doit inclure explicitement l'audit d'absence de fuite blind test ; `QA-001` reste `TODO` jusqu'à la clôture sécurité.
